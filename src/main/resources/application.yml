
# 网盘相关配置
spring:

  datasource:
    url: jdbc:mysql://localhost:3306/read?charset=utf8mb4&serverTimezone=UTC&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update

debug: false
